@model ERP_BIEN.ViewModels.AuditIndexViewModel

@{
    ViewData["Title"] = "Auditoría";

    // Acceso correcto a la QueryString en MVC
    var qUser = Context.Request.Query["user"];
    var qAction = Context.Request.Query["action"];
    var qEntity = Context.Request.Query["entity"];
    var qFrom = Context.Request.Query["from"];
    var qTo = Context.Request.Query["to"];
}

<h2 class="mb-3">Auditoría</h2>

<!-- ========================= -->
<!-- FILTROS -->
<!-- ========================= -->
<form method="get" class="card card-body mb-3">
    <div class="row g-3">

        <div class="col-md-3">
            <label class="form-label">Usuario</label>
            <input name="user" class="form-control" value="@qUser" placeholder="Nombre de usuario" />
        </div>

        <div class="col-md-2">
            <label class="form-label">Acción</label>
            <select name="action" class="form-select">
                <option value="">Todas</option>
                <option value="Create" @(qAction == "Create" ? "selected" : "")>Create</option>
                <option value="Edit" @(qAction == "Edit" ? "selected" : "")>Edit</option>
                <option value="Delete" @(qAction == "Delete" ? "selected" : "")>Delete</option>
                <option value="Assign" @(qAction == "Assign" ? "selected" : "")>Assign</option>
                <option value="Remove" @(qAction == "Remove" ? "selected" : "")>Remove</option>
            </select>
        </div>

        <div class="col-md-2">
            <label class="form-label">Entidad</label>
            <select name="entity" class="form-select">
                <option value="">Todas</option>
                <option value="Employee" @(qEntity == "Employee" ? "selected" : "")>Employee</option>
                <option value="Device" @(qEntity == "Device" ? "selected" : "")>Device</option>
                <option value="License" @(qEntity == "License" ? "selected" : "")>License</option>
                <option value="Role" @(qEntity == "Role" ? "selected" : "")>Role</option>
            </select>
        </div>

        <div class="col-md-2">
            <label class="form-label">Desde</label>
            <input type="date" name="from" class="form-control" value="@qFrom" />
        </div>

        <div class="col-md-2">
            <label class="form-label">Hasta</label>
            <input type="date" name="to" class="form-control" value="@qTo" />
        </div>

        <div class="col-md-1 d-flex align-items-end">
            <button class="btn btn-primary w-100">Filtrar</button>
        </div>

    </div>
</form>

<!-- ========================= -->
<!-- TABLA -->
<!-- ========================= -->
<div class="table-responsive">
    <table class="table table-hover align-middle">
        <thead class="table-light">
            <tr>
                <th>Fecha</th>
                <th>Usuario</th>
                <th>Acción</th>
                <th>Entidad</th>
                <th>Id</th>
                <th>Detalles</th>
            </tr>
        </thead>
        <tbody>
            @if (!Model.Logs.Any())
            {
                <tr>
                    <td colspan="6" class="text-center text-muted py-3">
                        No hay registros de auditoría
                    </td>
                </tr>
            }
            else
            {
                foreach (var log in Model.Logs)
                {
                    <tr>
                        <td>@log.Timestamp.ToString("yyyy-MM-dd HH:mm:ss")</td>
                        <td>@log.UserName</td>
                        <td>@log.Action</td>
                        <td>@log.Entity</td>
                        <td>@log.EntityId</td>
                        <td>@log.Details</td>
                    </tr>
                }
            }
        </tbody>
    </table>
</div>

<!-- ========================= -->
<!-- PAGINACIÓN -->
<!-- ========================= -->
@if (Model.TotalPages > 1)
{
    <nav aria-label="Page navigation" class="mt-3">
        <ul class="pagination justify-content-center">

            <li class="page-item @(Model.Page == 1 ? "disabled" : "")">
                <a class="page-link"
                   href="@Url.Action("Index", new {
                       page = Model.Page - 1,
                       pageSize = Model.PageSize,
                       user = qUser,
                       action = qAction,
                       entity = qEntity,
                       from = qFrom,
                       to = qTo
                   })">
                    Anterior
                </a>
            </li>

            @for (int i = 1; i <= Model.TotalPages; i++)
            {
                <li class="page-item @(i == Model.Page ? "active" : "")">
                    <a class="page-link"
                       href="@Url.Action("Index", new {
                           page = i,
                           pageSize = Model.PageSize,
                           user = qUser,
                           action = qAction,
                           entity = qEntity,
                           from = qFrom,
                           to = qTo
                       })">
                        @i
                    </a>
                </li>
            }

            <li class="page-item @(Model.Page == Model.TotalPages ? "disabled" : "")">
                <a class="page-link"
                   href="@Url.Action("Index", new {
                       page = Model.Page + 1,
                       pageSize = Model.PageSize,
                       user = qUser,
                       action = qAction,
                       entity = qEntity,
                       from = qFrom,
                       to = qTo
                   })">
                    Siguiente
                </a>
            </li>

        </ul>
    </nav>
}
